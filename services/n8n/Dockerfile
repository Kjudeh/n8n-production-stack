# n8n — pinned by tag AND digest; bumped only via reviewed PRs from the weekly
# upstream-check workflow. This image serves BOTH the main service and the
# queue workers (the worker service overrides the start command with "worker").
FROM n8nio/n8n:2.36.5@sha256:2cf92b86cb9d36def87b6d7abc19f6ec882f397c4f489248f170e3b782a810fc
EXPOSE 5678
